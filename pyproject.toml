[project]
name = "heptools"
version = "0.3.2"
requires-python = ">=3.10"
dependencies = [
    "rich",
    "numpy",
    "matplotlib",
    "dask-jobqueue",
# Scikit-HEP
    "awkward",
    "uproot",
    "hist",
# CMS
    "coffea",
    "correctionlib",
]
