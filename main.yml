name: heptools-dev
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pycurl
  # numerical
  - numpy
  - scipy
  - pandas
  # visualization
  - matplotlib
  - bokeh
  - graphviz
  - pip:
      - graphviz
      - pyvis
  - rich
  # parallel
  - dask-jobqueue

  # XRootD
  - xrootd
  - fsspec-xrootd
  # Scikit-HEP
  - awkward=1.10.3
  - uproot=4.3.7
  - hist
  - particle
  # CMS
  - coffea=0.7.22
  - correctionlib
  - pip:
      - dbs3-client
      - rucio-clients # Linux
